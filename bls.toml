name = "hello-world-0xeniac"
version = "1.0.0"

[deployment]
permission = "public"
nodes = 1

[build]
dir = "build"
entry = "hello-world-0xeniac_debug.wasm"
command = "npm run build:debug"

[build_release]
dir = "build"
entry = "hello-world-0xeniac.wasm"
command = "npm run build:release"
